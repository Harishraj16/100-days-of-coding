#include <stdio.h>

int main(void) {
	int t,a,b,c;
	scanf("%d",&t);
	while(t--){
	    scanf("%d%d%d",&a,&b,&c);
	    if(((b>=a)&&(b>=c))&&(a<=c || c<=a))
	    printf("YES\n");
	    else
	    printf("NO\n");
	}
	return 0;
}

